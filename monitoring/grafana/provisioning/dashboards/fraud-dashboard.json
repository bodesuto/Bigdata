{
  "annotations": { "list": [ { "builtIn": 1, "datasource": { "type": "grafana", "uid": "-- Grafana --" }, "enable": true, "hide": true, "name": "Annotations & Alerts", "type": "dashboard" } ] },
  "editable": true,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 8, "x": 0, "y": 0 },
      "id": 1,
      "options": { "colorMode": "value", "graphMode": "area", "justifyMode": "auto", "reduceOptions": { "calcs": ["lastNotNull"] }, "textMode": "auto" },
      "title": "Throughput (EPS)",
      "type": "stat",
      "targets": [ { "expr": "sum(rate({__name__=~\".*StreamingQuery_processedRecords_total.*\"}[1m])) or vector(0)", "refId": "A" } ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 8, "x": 8, "y": 0 },
      "id": 4,
      "options": { "colorMode": "value", "graphMode": "area", "justifyMode": "auto", "reduceOptions": { "calcs": ["lastNotNull"] }, "textMode": "auto" },
      "title": "Spark CPU Usage (%)",
      "type": "stat",
      "targets": [ { "expr": "sum(rate({__name__=~\".*cpuTime.*\"}[1m])) / 1000000 or vector(0)", "refId": "A" } ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 8, "x": 16, "y": 0 },
      "id": 5,
      "options": { "colorMode": "value", "graphMode": "area", "justifyMode": "auto", "reduceOptions": { "calcs": ["lastNotNull"] }, "textMode": "auto" },
      "title": "JVM Heap Memory (MB)",
      "type": "stat",
      "targets": [ { "expr": "sum({__name__=~\".*jvm_heap_used.*\"}) / 1048576 or vector(0)", "refId": "A" } ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 6 },
      "id": 3,
      "title": "System Resources & Performance",
      "type": "timeseries",
      "targets": [
        { "expr": "sum(rate({__name__=~\".*StreamingQuery_processedRecords_total.*\"}[1m]))", "legendFormat": "Throughput (EPS)", "refId": "A" },
        { "expr": "sum(rate({__name__=~\".*cpuTime.*\"}[1m])) / 1000000", "legendFormat": "CPU Usage (%)", "refId": "B" }
      ],
      "options": { "legend": { "displayMode": "table", "placement": "right" } }
    }
  ],
  "refresh": "5s",
  "schemaVersion": 38,
  "style": "dark",
  "title": "Fraud Detection Metrics v4",
  "uid": "fraud-detection-metrics",
  "version": 7
}
